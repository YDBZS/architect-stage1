<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.architect.mapper.CategoryMapperCustom">
    <resultMap id="myCategoryVO" type="com.architect.pojo.vo.CategoryVO">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="INTEGER"/>
        <result column="fatherId" property="fatherId"/>
        <!--
            collection 标签：定义关联的List集合类型的封装规则
            property：对应三级分类的list属性名
            ofType：集合的类型，三级分类的VO
        -->
        <collection property="subCatList" ofType="com.architect.pojo.vo.SubCategoryVO">
            <id column="subId" property="subId" jdbcType="INTEGER"/>
            <result column="subName" property="subName" jdbcType="VARCHAR"/>
            <result column="subType" property="subType" jdbcType="INTEGER"/>
            <result column="subFatherId" property="subFatherId"/>
        </collection>
    </resultMap>


    <select id="getSubCatList" parameterType="int" resultMap="myCategoryVO">
        select f.id        id,
               f.name      name,
               f.type      type,
               f.father_id fatherId,
               c.id        subId,
               c.name      subName,
               c.type      subType,
               c.father_id subFatherId
        from category f
         left join
             category c
         on
                 f.id = c.father_id
        where f.father_id = #{rootCatId,jdbcType=INTEGER}
    </select>

    <resultMap id="myNewItemsVO" type="com.architect.pojo.vo.NewItemsVO">
        <id column="rootCatId" property="rootCatId"/>
        <result column="rootCatName" property="rootCatName"/>
        <result column="slogan" property="slogan"/>
        <result column="catImage" property="catImage"/>
        <result column="bgColor" property="bgColor"/>
        <collection property="simpleItemList" ofType="com.architect.pojo.vo.SimpleItemVO">
            <id column="itemId" property="itemId"/>
            <result column="itemName" property="itemName"/>
            <result column="itemUrl" property="itemUrl"/>
        </collection>
    </resultMap>
    <select id="getSixNewItemsLazy" resultMap="myNewItemsVO" parameterType="map">
        select f.id           rootCatId,
               f.name         rootCatName,
               f.slogan,
               f.cat_image    catImage,
               f.bg_color     bgColor,
               i.id           itemId,
               i.item_name    itemName,
               ii.url         itemUrl,
               i.created_time createdTime
        from category f
         left join
         items i
         on
                 f.id = i.root_cat_id
         left join
         items_img ii
         on
                 i.id = ii.item_id
        where f.type = 1
          and i.root_cat_id = #{paramsMap.rootCatId}
          and ii.is_main = 1
        order by i.created_time desc limit 0,6
    </select>
</mapper>